export type MutedUser = {
	blocked_user_id: string;
	blocked_tweet_id: string;
	blocked_username: string;
	created_at: string;
	count: number;
};

export type MutedChart = {
	month: string;
	amount: number;
};
